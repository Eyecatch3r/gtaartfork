generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model FormData {
  id          Int     @id @default(autoincrement())
  Name        String?
  Occupation  String?
  Description String?
  image       String?
  Role        String?
}
